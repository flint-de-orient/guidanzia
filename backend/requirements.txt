# Pinned for reproducible installs. The app relies on the private
# google.generativeai.client._ClientManager API for per-key clients, so keep
# google-generativeai pinned exactly on upgrades.
flask==3.1.2
flask-cors==6.0.1
google-generativeai==0.8.5
google-api-core==2.25.1
requests==2.32.5
deep-translator==1.11.4
playwright==1.58.0
python-dotenv==1.1.1
nltk==3.9.2
pymongo==4.10.1
